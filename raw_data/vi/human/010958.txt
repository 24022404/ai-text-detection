Cấu hình p let host = "192.168.1.24" Cấu hình port app.http.server.confguraton.port = 8080 Cấu hình dung lượng lớn nhất của http body app.routes.defaultMaxBodySze = "20mb" Cấu hình tên database databaseName = "vapordatabase" Cấu hình port database databasePort = 5432 Cấu hình hostname database hostname: Envronment.get("DATABASEHOST") ?? "localhost" Cấu hình username database username: Envronment.get("DATABASEUSERNAME") ?? "vaporusername" Cấu hình password database password: Envronment.get("DATABASEPASSWORD") ?? "vaporpassword" Cấu hình MongoDB try app.databases.use(.mongo(connectonStrng: "mongodb://localhost:27017/mongo"), as: .mongo) try app.ntalzeMongoDB(connectonStrng: "mongodb://localhost:27017/mongo") Bắt điầu chạy phần mềm trên Xcode bằng tổ hợp phím command + R Trển kha http server trên Ubuntu:Cấu hình server trên Ubunto gống vớ hướng dẫn trên MacOS.