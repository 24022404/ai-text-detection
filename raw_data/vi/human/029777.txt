Mỗ tài liệu HTML bao gồm 1 bộ tag (hay còn gọi là element). Nó tạo ra một cấu trúc tương tự như cây thư mục với các headng, secton, paragraph,. . . và một số khố nộ dung khác. Hầu hết tất cả các HTML element điều có một tag mở và một tag đióng với cấu trúc <tag></tag>.4.1 Thiết kế kiến trúc 4.1.1 Lựa chọn kiến trúc phần mềm Trong đồ án, em sẽ sử dụng kiến trúc phân lớp (Layered Architecture), cụ thể là kiến trúc ba lớp cho hệ thống. Ba lớp cụ thể sẽ được tách bệt là lớp giao diện, lớp xử lý logc và lớp cơ sở dữ liệu. Cụ thể của mỗ lớp như sau: