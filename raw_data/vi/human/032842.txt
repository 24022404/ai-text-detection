//Analyst.java return df.select(splt(col("tme")," ).getItem(0).as("date"), col("browser"), col("gud")) .groupBy("date", "browser") .agg(count("gud").as("vew"), countDstnct("gud").as("user")) .collectAsLst(); Phân tích lưu lượng truy cập theo hệ điều hành Bà toán: Tính số lượng người dùng, lượt xem báo điện tử theo hệ điều hành hàng ngày.