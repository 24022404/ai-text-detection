STT Thuộc tính Kểu dữ lệu Ràng buộc NULL 1 projectd INTEGER Khóa ngoà NOT NULL 2 lognurl VARCHAR(255) NOT NULL 3 userparam VARCHAR(255) NOT NULL 4 passparam VARCHAR(255) NOT NULL 5 username VARCHAR(255) Yes 6 password VARCHAR(255) Yes 7 csrfparam VARCHAR(255) Yes Bảng 4.3: Bảng Sessons 4.Bảng requests : Mỗ dự án điể thực hiện kểm thử cần cha nhỏ thành các URL, bảng requests lưu trữ thông tn URL đió.Thông tn lưu trữ bao gồm:requestd, projectd, url, status(trạng thá của url), bug(có lỗ hay không), sscan ( điã kểm thử hay chưa) , haveparam ( có parameter không) ,...... Bảng request lên kết tớ bảng projects thông qua khóa ngoạ projectd. Bảng 4.4 thể hiện ch tết bảng Requests.STT Thuộc tính Kểu dữ lệu Ràng buộc NULL 1 requestd AUTOINCREMENT Khóa chính NOT NULL 2 projectd INTEGER Khóa ngoà NOT NULL 3 requesturl VARCHAR(255) NOT NULL 4 bug VARCHAR(255) NOT NULL 5 sscan INTEGER DEFAULT 0 Yes 6 haveparam VARCHAR(255) Yes 7 pentester INTERGER Khóa ngoà Yes Bảng 4.4: Bảng Requests 5.Bảng bugs : Lưu trữ thông tn về lỗ hổng trên từng request. Thông tn lưu trữ bao gồm : bugd, requestd, bug name (tên lỗ hổng),bugurl ( url bị lỗ), method (phương thức) , rsk ( mức điộ nguy hạ), descrpton ( mô tả lỗ), soluton ( gả pháp ngăn chặn), reference ( lên quan), pentester( người kểm thử) ..... Bảng bugs điược lên kết tớ bảng requests thông qua khóa ngoạ requestd và lên kết tớ bảng Users thông qua khóa ngoạ userd. Bảng 4.5 mô tả về bảng bugs.