Hàm extractData nhận điầu vào là một dòng nhật ký, có nhệm vụ trích xuất các trường dữ lệu cần thết và trả về dướ dạng một bản gh có cấu trúc như ở bảng 3.1 //LogProcessor.java Strng[] s = lne.splt("\t"); GenercData.Record record = new GenercData.Record(SCHEMA); record.put("tme", s); record.put("browser", Common.IntStr(s)); record.put("os", Common.IntStr(s)); record.put("loc", Common.IntStr(s)); record.put("doman", s); record.put("path", s); record.put("gud", Common.LongStr(s.replace("]", ))); record.put("category", s); return record; return null; Hàm parseRawToSchema điọc từng dòng nhật ký trong fle dữ lệu thô và gọ điến hàm extractData điể xử lý, tập hợp các bản gh thành một danh sách.