Kỹ thuật thứ ba được sử dụng là wndown. Kỹ thuật này có thể được mô tả thông qua hình 5.1.Hình 5.1: Hình ảnh mô tả kỹ thuật wndowng Kh danh sách có nhiều phần tử, việc hiển thị trực tiếp lên trình duyệt sẽ gây tốn tài nguyên và làm cho ứng dụng chạy chậm. Để gả quyết vấn đề này, kỹ thuật wndowng được sử dụng để tạo ra một khung nhìn ảo cho danh sách. Trong khung nhìn này, chỉ một số phần tử đầu tên của danh sách sẽ được hiển thị trên trang, trong khi các phần tử khác sẽ bị loại bỏ khỏi trình duyệt điển cảm tả cho trình duyệt. Trong khi đó, dữ liệu của danh sách này vẫn được lưu trong bộ nhớ. Kh người dùng cuộn xuống dưới của trang, các phần tử tiếp theo của danh sách sẽ thay thế các phần tử hiện tại trong khung nhìn. Điều này giúp tạo ra cảm giác như đang xem một danh sách già nhưng thực chất chỉ xem một phần của danh sách, giúp cho trình duyệt hoạt động mượt mà hơn và tốn ít tài nguyên hơn.