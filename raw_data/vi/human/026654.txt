Lớp CrawlWorkerServce Thuộc tính currencyThực thể lấy trong cơ sở dữ lệu điạ dện cho các thông số cấu hình điể kết nố vớ mạng chuỗ khố collectonThực thể lấy trong cơ sở dữ lệu điạ điạ dện cho thông tn của một bộ sưutập NFTnotfcatonSer vceThực thể của NotfcatonServce dùng điể thông báo trạng thá của hệ thống web3Thực thể dùng điể kết nố tớ mạng chuỗ khố, sử dụng RPC endpont lưu trong currency điể khở tạo nft20AbLưu trữ ABI của smart contract có cấu trúc ERC20 nft1155AbLưu trữ ABI của smart contract có cấu trúc ERC1155 nftContractThực thể khở tạo từ web3 và ABI tương ứng vớ collecton dùng điể kết nố tớ smart contract của bộ sưu tập NFT Phương thứcsetupKhở chạy phương thức doCrawlJob ở constructor doCrawlJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể crawl dữ lệu của bộ sưu tập NFT crawlDataKhở chạy crawl dữ lệu của bộ sưu tập NFT từ block number mà bộ sưu tập đió điược tạocrawlBlock721Lấy và xử lý dữ lệu của các transac tons xảy ra trên bộ sưu tập NFT có ABI là ERC20 từ mạng chuỗ khố và lưu vào cơ sở dữ lệucrawlBlock1155Lấy và xử lý dữ lệu của các transac tons xảy ra trên bộ sưu tập NFT có ABI là ERC1155 từ mạng chuỗ khố và lưu vào cơ sở dữ lệu web3CacheCache các thông tn lấy từ mạng chuỗ khố điể tố ưu số lần gọ RPC endpont nhằm gảm ch phí Bảng 4.1: Bảng ch tết Thuộc tính và Phương thức của lớp CrawlWorkerServce b, Lớp MarketWorkerServceThuộc tính currencyThực thể lấy trong cơ sở dữ lệu điạ dện cho các thông số cấu hình điể kết nố vớ mạng chuỗ khốnotfcatonSer vceThực thể của NotfcatonServce dùng điể thông báo trạng thá của hệ thống web3Thực thể dùng điể kết nố tớ mạng chuỗ khố, sử dụng RPC endpont lưu trong currency điể khở tạomatketAbLưu trữ ABI của smart contract Mar ket marketContractThực thể khở tạo từ web3 và ABI của smart contract Market dùng điể kết nố tớ smart contract đió Phương thứcsetupKhở chạy phương thức doCrawlJob ở constructor doCrawlJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể crawl dữ lệu của smart contract Market crawlDataKhở chạy crawl dữ lệu của bộ sưu tập NFT từ block number mà smart contract Market điược tạocrawlBlockLấy và xử lý dữ lệu của các transactons xảy ra trên smart contract Mar ket và lưu vào cơ sở dữ lệuhandleOrder CreatedEventsLưu trữ dữ lệu của event tạo điăng bán NFT vào cơ sở dữ lệuhandleOrderUp datedEventsLưu trữ dữ lệu của event cập nhật điăng bán NFT vào cơ sở dữ lệuhandleOrderCancelledE ventsLưu trữ dữ lệu của event hủy điăng bán NFT vào cơ sở dữ lệuhandle BuyEventsLưu trữ dữ lệu của event mua NFTđiã điược điăng bán vào cơ sở dữ lệuhandleSaveOderOn chanXử lý và lưu trữ hoặc cập nhật dữ lệu các sự kện điăng bán NFT vào cơ sở dữ lệu web3CacheCache các thông tn lấy từ mạng chuỗ khố điể tố ưu số lần gọ RPC endpont nhằm gảm ch phí Bảng 4.2: Bảng ch tết Thuộc tính và Phương thức của lớp MarketWorkerServce c, Lớp AuctonWorkerServce Thuộc tính currencyThực thể lấy trong cơ sở dữ lệu điạ dện cho các thông số cấu hình điể kết nố vớ mạng chuỗ khốnotfcatonSer vceThực thể của NotfcatonServce dùng điể thông báo trạng thá của hệ thống web3Thực thể dùng điể kết nố tớ mạng chuỗ khố, sử dụng RPC endpont lưu trong currency điể khở tạoauctonAbLưu trữ ABI của smart contract Auc ton auctonContractThực thể khở tạo từ web3 và ABI của smart contract Aucton dùng điể kết nố tớ smart contract đió Phương thứcsetupKhở chạy phương thức doCrawlJob ở constructor doCrawlJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể crawl dữ lệu của bộ sưu tập NFT crawlDataKhở chạy crawl dữ lệu của bộ sưu tập NFT từ block number mà bộ sưu tập đió điược tạocrawlBlockLấy và xử lý dữ lệu của các transactons xảy ra trên smart contract Auc ton và lưu vào cơ sở dữ lệuhandleAucton CreatedEventsLưu trữ dữ lệu của event tạo tn điấugá NFT vào cơ sở dữ lệuhandleAucton CanceledEventsLưu trữ dữ lệu của event hủy tn điấu gá NFT vào cơ sở dữ lệuhandleAuctonReclamedE ventsLưu trữ dữ lệu của event nhận lạ NFT điã điấu gá kh không có a điấu gá thành công vào cơ sở dữ lệuhandleBdAuctonCreatedE ventsLưu trữ dữ lệu của event người dùng tạo điấu gá (bd) NFT điã điược điăng tn điấu gá vào cơ sở dữ lệuhandleBdAuctonEdtedE ventsLưu trữ dữ lệu của event người dùng cập nhật gá điấu gá (bd) NFT vào cơ sở dữ lệuhandleBdAuctonCanceledE ventsLưu trữ dữ lệu của event người dùng hủy điấu gá (bd) NFT vào cơ sở dữ lệuhandleBdAuctonAcceptedE ventsLưu trữ dữ lệu của event người bán chấp nhận gá điấu gá (bd) NFT của người dùng có gá cao nhất vào cơ sở dữ lệuhandleBdAuctonClamedE ventsLưu trữ dữ lệu của event người dùng lấy NFT điã điấu gá thành công vào cơ sở dữ lệuhandleSaveAuc tonOnchanXử lý và lưu trữ hoặc cập nhật dữ lệu các sự kện điăng tn điấu gá NFT vào cơ sở dữ lệuhandleSaveBdAuctonOn chanXử lý và lưu trữ hoặc cập nhật dữ lệu các sự kện điấu gá (bd) NFT vào cơ sở dữ lệuprocessDataUp dateStatusXử lý và cập nhật dữ lệu, trạng thá của các tn điấu gá NFT kh quá trình điấu gá dễn ra vào cơ sở dữ lệugetCurrencyTo kenLấy thông tn cấu hình của token dùng điể thanh toán điể xử lý gá của điấu gá web3CacheCache các thông tn lấy từ mạng chuỗ khố điể tố ưu số lần gọ RPC endpont nhằm gảm ch phí Bảng 4.3: Bảng ch tết Thuộc tính và Phương thức của lớp AuctonWorkerServced, Lớp LootBoxWorkerServce Thuộc tính currencyThực thể lấy trong cơ sở dữ lệu điạ dện cho các thông số cấu hình điể kết nố vớ mạng chuỗ khốnotfcatonSer vceThực thể của NotfcatonServce dùng điể thông báo trạng thá của hệ thống web3Thực thể dùng điể kết nố tớ mạng chuỗ khố, sử dụng RPC endpont lưu trong currency điể khở tạolootBoxAbLưu trữ ABI của smart contract Loot BoxlootBoxCon tractThực thể khở tạo từ web3 và ABI của smart contract LootBox dùng điể kết nố tớ smart contract đió Phương thứcsetupKhở chạy phương thức doCrawlJob ở constructor doCrawlJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể crawl dữ lệu của bộ sưu tập NFT crawlDataKhở chạy crawl dữ lệu của bộ sưu tập NFT từ block number mà bộ sưu tập đió điược tạocrawlBlockLấy và xử lý dữ lệu của các transactons xảy ra trên smart contract Auc ton và lưu vào cơ sở dữ lệuhandleCreated LootBoxEventsLưu trữ dữ lệu của event tạo hộp quà bí mật vào cơ sở dữ lệuhandleRemovedLootBox EventsLưu trữ dữ lệu của event hủy hộp quà bí mật vào cơ sở dữ lệuhandleDepostedLootBox EventsLưu trữ dữ lệu của event chuyển NFTs vào hộp quà bí mật điã tạo thành công vào cơ sở dữ lệuhandleLootBox OpenedEventsLưu trữ dữ lệu của event mở hộp quà bí mật mà người dùng điã mua vào cơsở dữ lệuhandleUpdateLootBox OnchanXử lý và cập nhật dữ lệu của các hộp qua bí mật sau kh xử lý các events vào cơ sở dữ lệuupdateRartyN ftOnchanXử lý và cập nhật dữ lệu của các bảng lên quan tớ bảng hộp quà bí mật sau kh xử lý các events vào cơ sở dữ lệuprocessData ToUpdateStatusXử lý và cập nhật dữ lệu, trạng thá của các hộp quà bí mật và các dữ lệu lên quan kh từ lúc hộp quà bí mật điược tạo tớ lúc hộp quà bí mật điược mở dễn ra vào cơ sở dữ lệu web3CacheCache các thông tn lấy từ mạng chuỗ khố điể tố ưu số lần gọ RPC endpont nhằm gảm ch phí Bảng 4.4: Bảng ch tết Thuộc tính và Phương thức của lớp LootBoxWorkerServce e, Lớp ExchangeOn2OffWorkerServce Thuộc tính currencyThực thể lấy trong cơ sở dữ lệu điạ dện cho các thông số cấu hình điể kết nố vớ mạng chuỗ khốnotfcatonSer vceThực thể của NotfcatonServce dùng điể thông báo trạng thá của hệ thống socketServceThực thể của SocketServce dùng điể gử socket event vớ người dùng kmsServceThực thể của KmsServce dùng điể gọ phương thức xử lý các tác vụ mã hóa và gả mã dữ lệugameServerSer vceThực thể của GameServerServce dùng điể gọ phương thức hỗ trợ gao tếp vớ hệ thống game server của điố tác userRepostoryThực thể của Repostory điạ dện cho bảng User dùng điể điọc gh dữ lệucủa user vào cơ sở dữ lệulatestBlock RepostoryThực thể của Repostory điạ dện cho bảng LatestBlock dùng điể điọc gh dữ lệu block number trên mạng chuỗ khố vào cơ sở dữ lệutransactonRe questRepostoryThực thể của Repostory điạ dện cho bảng TransactonRequest dùng điể điọc gh dữ lệu một yêu cầu chuyển điổ token của người dùng vào cơ sở dữ lệutransactonOn chanRepostoryThực thể của Repostory điạ dện cho bảng TransactonOnchan dùng điể điọc gh dữ lệu trên mạng chuỗ khố tương ứng vớ yêu cầu chuyển điổ token của người dùng vào cơ sở dữ lệutransactonLo gRepostoryThực thể của Repostory điạ dện cho bảng TransactonLog dùng điể gh dữ lệu trên mạng chuỗ khố tương ứng vớ yêu cầu chuyển điổ token của người dùng vào cơ sở dữ lệutokenRepos toryThực thể của Repostory điạ dện cho bảng Token dùng điể điọc gh dữ lệu của cấu hình token vào cơ sở dữ lệu web3Thực thể dùng điể kết nố tớ mạng chuỗ khố, sử dụng RPC endpont lưu trong currency điể khở tạoexchangeAbLưu trữ ABI của smart contract Ex changetokenAbLưu trữ ABI của smart contract To ken điạ dện cho token có chuẩn ERC20exchangeCon tractThực thể khở tạo từ web3 và ABI của smart contract Exchange dùng điể kết nố tớ smart contract đió Phương thứcsetupKhở chạy phương thức doCrawlJob ở constructordoCrawlJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể crawl dữ lệu của bộ sưu tập NFT doJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể xử lý quá trình chuyển điổ token crawlDataKhở chạy crawl dữ lệu của bộ sưu tập NFT từ block number mà bộ sưu tập đió điược tạocrawlBlockLấy và xử lý dữ lệu của các transactons xảy ra trên smart contract Auc ton và lưu vào cơ sở dữ lệuhandleDe postEventsLưu trữ và xử lý dữ lệu của event người dùng chuyển tền vào smartcontract điể thực hiện chuyển điổ to kenncreaseOf fchanBalanceXử lý gọ gameServerServce điể tăng lượng token chuyển điổ tương ứng cho người dùngverfyIncreaseOffchanRe questXác nhận trạng thá của yêu cầu tăng lượng token chuyển điổ của người dùng getGasPrceLấy gá gas điể thực hiện một gao dịch trên mạng chuỗ khố web3CacheCache các thông tn lấy từ mạng chuỗ khố điể tố ưu số lần gọ RPC endpont nhằm gảm ch phíBảng 4.5: Bảng ch tết Thuộc tính và Phương thức của lớp Ex changeOn2OffWorkerServce f, Lớp ExchangeOff2OnWorkerServce Thuộc tính currencyThực thể lấy trong cơ sở dữ lệu điạ dện cho các thông số cấu hình điể kết nố vớ mạng chuỗ khốnotfcatonSer vceThực thể của NotfcatonServce dùng điể thông báo trạng thá của hệ thốngsocketServceThực thể của SocketServce dùng điể gử socket event vớ người dùng kmsServceThực thể của KmsServce dùng điể gọ phương thức xử lý các tác vụ mã hóa và gả mã dữ lệugameServerSer vceThực thể của GameServerServce dùng điể gọ phương thức hỗ trợ gao tếp vớ hệ thống game server của điố tác userRepostoryThực thể của Repostory điạ dện cho bảng User dùng điể điọc gh dữ lệu của user vào cơ sở dữ lệulatestBlock RepostoryThực thể của Repostory điạ dện cho bảng LatestBlock dùng điể điọc gh dữ lệu block number trên mạng chuỗ khố vào cơ sở dữ lệutransactonRe questRepostoryThực thể của Repostory điạ dện cho bảng TransactonRequest dùng điể điọc gh dữ lệu một yêu cầu chuyển điổ token của người dùng vào cơ sở dữ lệutransactonOn chanRepostoryThực thể của Repostory điạ dện cho bảng TransactonOnchan dùng điể điọc gh dữ lệu trên mạng chuỗ khố tương ứng vớ yêu cầu chuyển điổ token của người dùng vào cơ sở dữ lệutransactonLo gRepostoryThực thể của Repostory điạ dện cho bảng TransactonLog dùng điể gh dữ lệu trên mạng chuỗ khố tương ứng vớ yêu cầu chuyển điổ token của người dùng vào cơ sở dữ lệutokenRepos toryThực thể của Repostory điạ dện cho bảng Token dùng điể điọc gh dữ lệu của cấu hình token vào cơ sở dữ lệuweb3Thực thể dùng điể kết nố tớ mạng chuỗ khố, sử dụng RPC endpont lưu trong currency điể khở tạoexchangeAbLưu trữ ABI của smart contract Ex changetokenAbLưu trữ ABI của smart contract To ken điạ dện cho token có chuẩn ERC20exchangeCon tractThực thể khở tạo từ web3 và ABI của smart contract Exchange dùng điể kết nố tớ smart contract đió Phương thứcsetupKhở chạy phương thức doCrawlJob ở constructor doCrawlJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể crawl dữ lệu của bộ sưu tập NFT doJobKhở chạy phương thức crawlData trong vòng lặp vô hạn điể xử lý quá trình chuyển điổ token crawlDataKhở chạy crawl dữ lệu của bộ sưu tập NFT từ block number mà bộ sưu tập đió điược tạocrawlBlockLấy và xử lý dữ lệu của các transactons xảy ra trên smart contract Auc ton và lưu vào cơ sở dữ lệuhandleUnlockedEventsLưu trữ dữ lệu của event smart con tract chuyển token cho người dùng sau kh người dùng thực hiện chức năng chuyển điổ token từ offchan sang onchanncreaseOf fchanBalanceXử lý gọ gameServerServce điể tăng lượng token chuyển điổ tương ứng cho người dùngverfyDecreaseOffchanRe questXác nhận trạng thá của yêu cầuchuyển điổ token offchan sang onchan của người dùngconstructRaw TransactonCấu trúc transacton điể chuyển token onchan cho người dùngreconstructRawTransactonTá cấu trúc transacton điể chuyển to ken onchan cho người dùng nếu có lỗ xảy ra kh tương tác vớ mạng chuỗ khốsgnRawTrans actonMaster wallet ký vào transacton điã điược cấu trúc điể có thể thực th trên mạng chuỗ khốsendRawTrans actonGử transacton điã điược cấu trúc lên mạng chuỗ khốgetTransacton StatusXác nhận và cập nhật trạng thá của transacton trên mạng chuỗ khố getGasPrceLấy gá gas điể thực hiện một gao dịch trên mạng chuỗ khố web3CacheCache các thông tn lấy từ mạng chuỗ khố điể tố ưu số lần gọ RPC endpont nhằm gảm ch phíBảng 4.6: Bảng ch tết Thuộc tính và Phương thức của lớp Exchange Off2OnWorkerServce 4.2.2 Thết kế ch tết Hợp đồng thông mnh a, Hợp đồng thông mnh Market Hợp đồng thông mnh Market có nhệm vụ xử lý vệc mua bán và chuyển điổ NFT của người mua sang người bán cũng như ngược lạ. Dướ điây là một số thuộc tính và phương thức quan trọng của hợp đồng: