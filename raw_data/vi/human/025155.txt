//LogProcessor.java publc Lst<GenercData.Record> parseRawToSchema(Strng Lst<Generic Data.Record> parquet = new ArrayList<>(); try (BufferedReader br = new BufferedReader(new String line; General Data.Record record; record = extract Data PC(lne); record = extract Data();of (record != null) parquet.add(record); e.printStackTrace(); return parquet; Hàm wrteParquetFle nhận điầu vào là một danh sách bản ghi có cấu trúc và một đường dẫn, thực hiện lưu dữ liệu ra file parquet theo đường dẫn điã cho ở HDFS.