Hiện nay trong kh hầu hết các phương pháp xây dựng phân cụm tự điộng chỉ dựa vào dữ lệu văn bản , , , vệc kết hợp các cấu trúc mạng mang lạ các thông tn lên quan, bổ sung có gá trị cho văn bản. Hãy cùng xem xét ví dụ sau: từ khóa "natural language processng" (xử lý ngôn ngữ tự nhên) thường xuất hiện cùng vớ từ "open source" (mã nguồn mở). Nếu chỉ dựa vào dữ lệu văn bản có thể xếp cụm từ này vào nhóm các từ lên quan điến mã nguồn mở. Tuy nhên nhờ có thông tn nhúng trong cấu trúc mạng, ví dụ như các hộ nghị lên quan (ví dụ, "ACL", "EMNLP" ), và các tác gả (ví dụ, "Chrstopher mannng", Yoshua Bengo" ) cho thấy mố lên hệ chặt chẽ gữa cụm từ "natural language processng" và học máy hoặc xử lý ngôn ngữ tự nhên (NLP). Đều này cho phép ta gán cụm từ vào taxonomy phù hợp. Vì vậy thuật toán "NetTaxo: Automated Topc Taxonomy Constructon from TextRch Network" thể hiện ưu điểm hơn so vớ các nghên cứu thuật toán xây dựng taxonomy tự điộng khác. Tuy nhên cũng cần lưuNETWORK ý rằng, một thuật ngữ có thể thuộc về nhều chủ điề khá nệm khác nhau và vì vậy có thể xuất hiện ở trong nhều nút khác nhau. Ví dụ "deep learnng" (học sâu) có thể là một phần của cả "machne learnng" (ví dụ trong câu "deep learnng theory n machne learnng") và "computer vson" (ví dụ trong câu "deep learnng n computer vson"). Một ví dụ khác là vớ từ "data stream" (luồng dữ lệu) có thể thuộc về "database" (ví dụ: "stream data ndexng n database") và thuộc về "data mnng" (ví dụ: "stream data classfcaton n data mnng").