− Đặc điểm Sự nhất quán trong cả API Tồn tại không trạng thái (ví dụ, không có server side session) Sử dụng HTTP status code ko cần thiết Sử dụng URL endpoint vớ logical hierarchy  Hình 1.4: Cấu trúc REST Versioning trong URL chứ không phải trong HTTP header − REST là một phương thức nhỏ gọn. Nên rất được ưa chuộng cho dữ liệu HTTP. Cũng vì vậy nên REST dần phổ biến trên web, và đi được xem là lựa chọn “số một” cho phát triển API.