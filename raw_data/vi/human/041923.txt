Nhận diện khuôn mặt với ResNet và Arc Fake a, ResNet ResNet , viết tắt của Residual Network, là một loại CÂN sử dụng "kết nối bỏ qua" hoặc "kết nối lối tắt" để giải quyết vấn đề gradient biến mất liên quan đến các mạng neural sâu. Trong các mạng neural truyền thống, gradient, được sử dụng trong quá trình lan truyền ngược để cập nhật trọng số, có thể trở nên ngày càng nhỏ kh được lan truyền ngược qua các lớp. Vấn đề này có thể làm cho việc huấn luyện các mạng sâu trở nên thách thức.