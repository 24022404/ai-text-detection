ˆx=x´⊕te()(3.4) Môđun hậu xử lý: Trong mô đun này, em sử dụng lớp mạng tuyến tính (Linear), Batch Normalization liên tiếp nhau điểm có thể giúp phân loại được nhãn của hộp văn bản trong tài liệu. Em sử dụng lớp mạng Batch Normalization ở giữa hai lớp mạng tuyến tính (Linear) để tránh hiện tượng quá khớp, tăng tốc độ học kh huấn luyện mô hình mạng nơron đề xuất. Mô hình của môđun hậu xử lý này giống với Perceptron Đa tầng (MLP), đầu vào của mạng MLP là vectơ ˆxdo môđun trước truyền tớ, đầu ra của mạng MLP là vectơ chứa nhiều với là số lớp cần phân loại của bài toán. Ở lớp mạng cuối cùng, đi qua hàm softmax thu được xác xuất của những lớp của mẫu t, từ đó, lấy ra lớp có xác xuất lớn nhất.