The structure of a convolutional neural network A convolutional neural network is a collection of convolutional layers that are superimposed and uses nonar activation functions such as ReLU and tanh to activate the weights in the node. This class, after passing the function, will be weighted in the nodes. These classes, after having passed the activation function, can generate more abstract information for the next classes.