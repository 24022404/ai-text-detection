RCutMix . Another method is based on CutMix , which randomly cutting and pasting a region from the target image onto the source image. In our method, we cut a region so that more pixels belong to class cthan 50% of the total number of pixels belong to class cin the source image. Formally, we randomly select class cwith probability function fcand then choose a image xAcontain this class. Wesample the bounding box coordinates B= (rx, ry, rw, rh)indicating the cropping regions on xA. The region needs to satisfy the condition: