Testing . At the test step, we resize the validation photos to 1024 x 512 for the input image. The teacher’s network M′and projection head Hare all immediately eliminated from the basic segmentation model, with no additional inference step added. We use per-class intersection-over-union (IoU), a widely used evaluation metric in semantic segmentation, as well as mean IoU across all classes.