pi(2j+ 1) = cos(i 100002i p) (2.9) pi(2j) = sin(i 100002j p) (2.10)Figure 2.4: Multi-head Attention from  b, Multi-head Attention After positional encoding, data are fed to a multi-head attention module with self-attention. The multi-head attention is illustrated in 2.4. This module applies the attention mechanism for htimes. The reason for multiple repetitions of attention can be elucidated as follows. The first attention pass determines the extent to which each word attends to other words. Subsequently, the second attention pass computes the degree to which each pair of words attends to other pairs of words.