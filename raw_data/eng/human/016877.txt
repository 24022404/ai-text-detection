Results and future development directions After research and implementation, I have built a micro-service system with theJava Spring Boot framework. Spring Boot makes it very easy to deploy micro services. Each micro-service system must have a gateway, which is a softwareapplication between a client and a set of back-end micro-services. I use cloud gateway, a package of maven that helps easily to create the configuration of the gateway.