The Spinner class and ImageScanner class are two sub-classes of the Launcherclass. They provide the user with an input channel to specify their desired destina tion and current position, respectively. The Spinner class allows the user to select a destination from a drop-down menu, while the ImageScanner class uses ARCore technology to scan an image and determine the user’s current position.