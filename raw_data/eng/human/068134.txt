Despite the efficacy in the open-vocabulary NMT, BPE tokenization segments a word into a unique sequence of tokens whereas there exists different segmentation candidates. Because the model only see the sequences of ids, while these sequences encode the same input, NMT handles them as completely different inputs. Therefore, training a NMT model with different segmentation candidates improve the robustness of the model .  proposed BPE-dropout which introduces stochastic corruptions in the segmentation procedure of BPE, which leads to producing multiple segmentation candidates within the same fixed BPE framework.