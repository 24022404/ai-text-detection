The results obtained using the 34k-vocab tokenizer are indeed promising, and it is noteworthy that it is a need to experiment with other tokenization approaches as well, including pre-segmentation before training the tokenizers and increasing the vocabulary size to 64k (the same size as Phobert). These additional experiments are valuable contributions and can significantly improve the model’s performance.