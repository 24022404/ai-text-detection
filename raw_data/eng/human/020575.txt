Support for login on multiple browsers and devices: JWT allows users to log in on different browsers and devices without losing the login session on the old browser.6.3 Clean code 6.3.1 Problem description Placing queries in a loop can cause performance problems, especially when there is a large number of iterations. For each iteration, the system will have to perform a new question, which consumes resources and prolongs processing time.