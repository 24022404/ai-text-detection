When the user logs in successfully, the server will return the JWT which is an encrypted string from three parts: header, payload, and signature. In it, the header is the type of the JWT token and the SH512 signing algorithm. The payload contains user authentication information, including login email, login method, authorities, and expiration date. The end of the JWT is the signature, encrypted from a private string of characters using the HMAC512 algorithm .