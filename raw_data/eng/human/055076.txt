JSON Path to Real-World Path Transformation The task of converting the path data from an API into a usable format for AR navigation presents a significant challenge. Unlike other AR navigation apps that must continuously detect each frame to render the 3D arrows one by one, ARNav can build the entire path at once, once all conditions are met. One of the keyconditions is converting the distances between points in the path data into real world distances that the camera can comprehend. This is accomplished by utilizing the Great Circle Theorem. The formula for the Great Circle Theorem is as follows: