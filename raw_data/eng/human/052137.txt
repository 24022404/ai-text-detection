The graphBuilder() method reads the JSON file of the map and constructs a weighted graph in the form of an adjacent matrix. The resulting matrix is passed as input to the pathFinding() method, which runs the Floyd-Warshall algorithm once to obtain all the shortest paths between all possible pairs of vertices in the graph. The resulting Python dictionary is then passed through the parseSave() method, which converts it into JSON data and calls the Model package for further processing.