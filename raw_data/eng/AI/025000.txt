The greedy construction algorithm forms a sequence by continually applying a greedy rule to extend an initial seed string, halting when it is impossible to add another symbol without creating a repeated substring of a given length k or upon meeting another stopping criterion. Interestingly, the specific greedy rule chosen can significantly affect the resulting algorithm, as each rule may result in distinct constructed sequences. Regardless of the rule, such methods consistently demand exponential space.