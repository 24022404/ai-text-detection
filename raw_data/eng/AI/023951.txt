Docker is employed in this thesis to encapsulate the application and all its dependencies within a portable container image, which guarantees consistency and reliability when deploying across different environments. By standardizing the packaging process, Docker reduces the likelihood of environment-specific bugs and streamlines the workflow. Additionally, Docker Compose is utilized to define and orchestrate the various services required during development, further simplifying and accelerating environment setup, enabling developers to efficiently spin up all needed components with minimal configuration.