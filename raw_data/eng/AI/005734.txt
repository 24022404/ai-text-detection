A Docker container is a lightweight, standalone executable package that runs as an isolated instance of a Docker image. By sharing the host OS kernel but maintaining its own filesystem, processes, and network, it provides consistency across multiple environments.