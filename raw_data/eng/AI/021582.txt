The addition of the function to artificial neural networks enables them to capture complex, real-world data patterns by incorporating nonlinearity into the model. In basic networks, each input (x) is multiplied with an associated weight (w), and the resulting value is processed by the function f(x). This operation transforms the input and allows the network to produce outputs for either the final result or as input for subsequent layers.